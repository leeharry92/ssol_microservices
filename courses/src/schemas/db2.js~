
var mongoose = require( 'mongoose' );
var Schema   = mongoose.Schema;

//module.exports = function() {
	var test_model2 = new Schema({
		user_id    : String,
		name	   : String,
		content    : [String],
		updated_at : Date
	});

	mongoose.model( 'test_model2', test_model2 ); // model
	var db2 = mongoose.createConnection('mongodb://localhost/db2');
	db2.on('error', function(err){
	  if(err) throw err;
	});

	db2.once('open', function callback () {
	  console.info('Mongo db connected successfully on: '+mongoServer);
	});

	module.exports = db2;
//};


